

const domain = "http://localhost:8000"

export const enviroment = {
    helpData: domain + "/guide",
    directory: domain + "/directory",
    files: domain + "/file",
    perceptron: domain + "/perceptron",
    analytical: domain + "/analytical"
}